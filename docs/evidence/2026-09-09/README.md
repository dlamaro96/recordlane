# Validation evidence — 2026-09-09

Evidence files are generated from reproducible scripts and contain only synthetic data and sanitized environment facts. A pass means the named command asserted stored or user-visible outcomes; missing or blocked acceptance gates remain explicit in `REQUIREMENTS.yaml` and `RELEASE_READINESS.md`. `continuation-validation.md` identifies the tested commit, environment, PostgreSQL-specific runs, local image and contract digests, and remaining blockers.
